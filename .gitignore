# Rust / Cargo
target
**/target/

# Asili build outputs
*.asb
*.asm
kilele/

# IDE / editor
.idea/
*.swp
*.swo
*~
.project
.settings/
*.sublime-*

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Debug / temp
*.log
*.tmp
*.temp
.cache/

# Node (extensions, LSP tooling)
node_modules/

# Packaged VS Code extension (build artifact, published via GitHub Releases instead)
extensions/vscode/*.vsix

# Generated API docs (rustdoc + TypeDoc), assembled by CI and published to GitHub Pages
docs-site/

# wasm-pack build output for the browser playground (build with examples/playground/build.sh)
examples/playground/pkg/

# Local env (secrets)
.env
.env.local
.env.*.local

# Claude Code local session state
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# stdlib surface stubs

Initial draft modules aligned to spec decisions:
- `moduli.asi` / `moduli.as` (root export surface)
- `hisabati.asi` / `hisabati.as`
- `mfumo.asi` / `mfumo.as`

These are interface-first placeholders, not complete implementations.

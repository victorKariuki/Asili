# cross_package

Demonstrates a `[tegemezi]` path dependency: `app` imports a struct (`Punkt`) and a
public constant (`PI`) from the sibling `mathutil` package via `leta mathutil`.

Run from `app/`:

```bash
cd app
pata jenga --tenda
```
